function display_HTML() {
    let myheading = '<h1>Hello WOrld</h1>';
    let mytext = `<p>while it was nice to know you worldthere are 
        only some things I am comfortable with sharing in a global context
        you cant alter the variable that holds this text outside of the block.
    </p>`; 
    document.write(myheading + mytext);
}
display_HTML(); 